<UserControl x:Class="BenkyoKanji.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:BenkyoKanji.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <UserControl.Resources>
        <conv:JlptLevelToColorConverter x:Key="LevelColorConv" />
        <conv:BoolToVisibilityConverter x:Key="BoolVisConv" />
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Row 0: Welcome Header & Quick Actions -->
            <Grid Grid.Row="0" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <Border Style="{StaticResource TailscalePill}" Background="{DynamicResource AccentBadgeBgBrush}" BorderBrush="{DynamicResource AccentBrush}" Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Style="{StaticResource StatusDot}" Fill="{DynamicResource AccentBrush}"/>
                                <TextBlock Text="에빙하우스 망각 곡선 가동 중" FontSize="11" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource TailscalePill}" Background="{DynamicResource SuccessBadgeBgBrush}" BorderBrush="{DynamicResource SuccessBrush}">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconFlame}" Fill="{DynamicResource SuccessBrush}" Width="10" Height="10" Stretch="Uniform" Margin="0,0,4,0"/>
                                <TextBlock Text="{Binding StreakDays, StringFormat='연속 {0}일 학습 중'}" FontSize="11" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    <TextBlock Text="오늘의 일본어 한자 학습 대시보드" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="망각 곡선 주기에 맞춰 기억이 흐려지기 전 최적의 타이밍에 복습하세요." FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,3,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}" Margin="0,0,8,0" ToolTip="새로고침">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource IconRefresh}" Fill="{DynamicResource TextSecondaryBrush}" Width="12" Height="12" Stretch="Uniform" Margin="0,0,6,0"/>
                            <TextBlock Text="새로고침" FontSize="12"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource PrimaryButton}" Command="{Binding StartReviewCommand}">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource IconStudy}" Fill="White" Width="14" Height="14" Stretch="Uniform" Margin="0,0,8,0"/>
                            <TextBlock Text="오늘 복습 시작하기" FontSize="13" FontWeight="Bold"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Row 1: Key Metric Cards (Tailscale 4-column cards) -->
            <Grid Grid.Row="1" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Card 1: Today Due Reviews -->
                <Border Grid.Column="0" Style="{StaticResource TailscaleCard}">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="오늘 복습 대기" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="SemiBold"/>
                            <Border HorizontalAlignment="Right" Background="{DynamicResource DangerBadgeBgBrush}" CornerRadius="6" Padding="6,2">
                                <TextBlock Text="DUE" FontSize="9" Foreground="{DynamicResource DangerBrush}" FontWeight="Bold"/>
                            </Border>
                        </Grid>
                        <TextBlock Text="{Binding DueReviewCount}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource DangerBrush}"/>
                        <TextBlock Text="개 한자 복습 필요" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Card 2: New Learning Pool -->
                <Border Grid.Column="2" Style="{StaticResource TailscaleCard}">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="새로 배울 한자" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="SemiBold"/>
                            <Border HorizontalAlignment="Right" Background="{DynamicResource AccentBadgeBgBrush}" CornerRadius="6" Padding="6,2">
                                <TextBlock Text="NEW" FontSize="9" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold"/>
                            </Border>
                        </Grid>
                        <TextBlock Text="{Binding NewLearningCount}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/>
                        <TextBlock Text="개 신규 단어 대기" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Card 3: Memory Retention Rate -->
                <Border Grid.Column="4" Style="{StaticResource TailscaleCard}">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="기억 정답률 (망각 억제)" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="SemiBold"/>
                            <Border HorizontalAlignment="Right" Background="{DynamicResource SuccessBadgeBgBrush}" CornerRadius="6" Padding="6,2">
                                <TextBlock Text="SRS" FontSize="9" Foreground="{DynamicResource SuccessBrush}" FontWeight="Bold"/>
                            </Border>
                        </Grid>
                        <TextBlock Text="{Binding RetentionRate, StringFormat='{}{0:F1}%'}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}"/>
                        <TextBlock Text="망각 곡선 유지율" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Card 4: Mastered / Total Cards -->
                <Border Grid.Column="6" Style="{StaticResource TailscaleCard}">
                    <StackPanel>
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="완전 암기 한자" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="SemiBold"/>
                            <Border HorizontalAlignment="Right" Background="{DynamicResource BgCardSecondaryBrush}" CornerRadius="6" Padding="6,2">
                                <TextBlock Text="TOTAL" FontSize="9" Foreground="{DynamicResource TextSecondaryBrush}" FontWeight="Bold"/>
                            </Border>
                        </Grid>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding MasteredCount}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <TextBlock Text="{Binding TotalCards, StringFormat='/ {0}'}" FontSize="18" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Bottom" Margin="6,0,0,4"/>
                        </StackPanel>
                        <TextBlock Text="장기 기억 도달 단어" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Row 2: Mastery Breakdown & Next 7-Day Forecast -->
            <Grid Grid.Row="2" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.2*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Mastery Status Bar Card -->
                <Border Grid.Column="0" Style="{StaticResource TailscaleCard}">
                    <StackPanel>
                        <TextBlock Text="한자 숙련도 단계별 현황" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>

                        <!-- Visual Progress Segment -->
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Background="#3b82f6" Height="8" CornerRadius="4,0,0,4" Margin="0,0,2,0"/>
                            <Border Grid.Column="1" Background="#f59e0b" Height="8" Margin="2,0,2,0"/>
                            <Border Grid.Column="2" Background="#8b5cf6" Height="8" Margin="2,0,2,0"/>
                            <Border Grid.Column="3" Background="#10b981" Height="8" CornerRadius="0,4,4,0" Margin="2,0,0,0"/>
                        </Grid>

                        <UniformGrid Columns="4">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                    <Ellipse Width="7" Height="7" Fill="#3b82f6" Margin="0,0,6,0"/>
                                    <TextBlock Text="미학습" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding NewCount, StringFormat='{}{0}개'}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                            </StackPanel>

                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                    <Ellipse Width="7" Height="7" Fill="#f59e0b" Margin="0,0,6,0"/>
                                    <TextBlock Text="초기 학습" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding LearningCount, StringFormat='{}{0}개'}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                            </StackPanel>

                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                    <Ellipse Width="7" Height="7" Fill="#8b5cf6" Margin="0,0,6,0"/>
                                    <TextBlock Text="주기 복습" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding ReviewingCount, StringFormat='{}{0}개'}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                            </StackPanel>

                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                    <Ellipse Width="7" Height="7" Fill="#10b981" Margin="0,0,6,0"/>
                                    <TextBlock Text="완전 숙련" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding MasteredCount, StringFormat='{}{0}개'}" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <!-- 7-Day Forecast Card -->
                <Border Grid.Column="2" Style="{StaticResource TailscaleCard}">
                    <StackPanel>
                        <TextBlock Text="향후 7일간 복습 일정 예보" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                        <ItemsControl ItemsSource="{Binding ForecastDays}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="1"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="2" Padding="6,8" CornerRadius="6" Background="{DynamicResource BgCardSecondaryBrush}">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding DayName}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                                            <TextBlock Text="{Binding DateStr}" FontSize="9" Foreground="{DynamicResource TextMutedBrush}" HorizontalAlignment="Center" Margin="0,2,0,4"/>
                                            <TextBlock Text="{Binding DueCount}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Row 3: Quick Action Hub & Due Kanji Preview -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left: Quick Workflow Shortcuts -->
                <Border Grid.Column="0" Style="{StaticResource TailscaleCard}">
                    <StackPanel>
                        <TextBlock Text="스마트 학습 바로가기" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>

                        <!-- PDF Worksheet Maker Shortcut -->
                        <Border Style="{StaticResource TailscaleSecondaryCard}" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="{DynamicResource AccentBadgeBgBrush}" Width="36" Height="36" CornerRadius="8" Margin="0,0,12,0">
                                    <Path Data="{StaticResource IconPdf}" Fill="{DynamicResource AccentBrush}" Width="18" Height="18" Stretch="Uniform"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="종이 출력용 PDF 시험지 만들기" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock Text="한자/음훈독/뜻 빈칸 퀴즈 표 생성 및 인쇄" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                                </StackPanel>
                                <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" Command="{Binding GenerateWorksheetCommand}" Content="생성하기" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <!-- Photo Grading Shortcut -->
                        <Border Style="{StaticResource TailscaleSecondaryCard}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="{DynamicResource SuccessBadgeBgBrush}" Width="36" Height="36" CornerRadius="8" Margin="0,0,12,0">
                                    <Path Data="{StaticResource IconGrading}" Fill="{DynamicResource SuccessBrush}" Width="18" Height="18" Stretch="Uniform"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="작성한 시험지 사진 채점 (OCR)" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    <TextBlock Text="손글씨 자동 채점 후 망각 곡선 데이터에 반영" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,2,0,0"/>
                                </StackPanel>
                                <Button Grid.Column="2" Style="{StaticResource SecondaryButton}" Command="{Binding OpenGradingCommand}" Content="사진 채점" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Right: Due Kanji Preview List -->
                <Border Grid.Column="2" Style="{StaticResource TailscaleCard}">
                    <StackPanel>
                        <TextBlock Text="오늘 복습 대기 중인 한자 미리보기" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                        <ItemsControl ItemsSource="{Binding QuickReviewPreview}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,6" Padding="10,8" Background="{DynamicResource BgCardSecondaryBrush}" CornerRadius="6">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="40"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Kanji}" FontFamily="{StaticResource KanjiFont}" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8,0,0,0">
                                                <TextBlock Text="{Binding MeaningKo}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                                <TextBlock Text="{Binding AllReadings}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                            </StackPanel>
                                            <Border Grid.Column="2" CornerRadius="6" Padding="6,2" VerticalAlignment="Center"
                                                    Background="{Binding Level, Converter={StaticResource LevelColorConv}}">
                                                <TextBlock Text="{Binding LevelDisplay}" FontSize="9" Foreground="White" FontWeight="Bold"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

        </Grid>
    </ScrollViewer>
</UserControl>
